on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: test
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: test step
        id: hello
        uses: ./
        with:
          SECRETS_NAME: 'Getpaid-frontend-develop'
