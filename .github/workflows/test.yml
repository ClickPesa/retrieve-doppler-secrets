on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: test
    steps:
      - uses: actions/checkout@v2
      - name: test step
        id: hello
        uses: ./
        with:
          SECRETS_NAME: 'Getpaid-frontend-develop'
