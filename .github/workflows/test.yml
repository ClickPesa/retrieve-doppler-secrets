on: [push]

jobs:
  test: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: test step
        id: hello
        uses: ./
        with:
          DOPPLER_TOKEN: ${{secrets.DOPPLER_TOKEN}}
      - name: test variables
        run: |
          echo "url ${{ env.REACT_APP_REST_URL }}"
          echo "env ${{ env.DOPPLER_ENVIRONMENT }}"
          echo "env ${{ env.DOPPLER_CONFIG }}"
