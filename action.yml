# action.yml
name: 'retrieve-doppler-secrets'
description: ''
inputs:
  DOPPLER_TOKEN: 
    description: 'doppler token'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.DOPPLER_TOKEN }}